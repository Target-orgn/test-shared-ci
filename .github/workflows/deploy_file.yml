name: rani-ukamble-group/deploy_file
on:
  workflow_call:
jobs:
  deploy_file:
    runs-on: ubuntu-latest
    container:
      image: alpine:latest
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v4.1.0
      with:
        fetch-depth: 20
    - run: echo "Deploying.."
